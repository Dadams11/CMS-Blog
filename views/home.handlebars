<!DOCTYPE html>
<html>
<head>
    <title>Home</title>
    <!-- Add your CSS links here -->
</head>
<body>
    <nav>
        <a href="/">Home</a>
        {{#if loggedIn}}
        <a href="/dashboard">Dashboard</a>
        <a href="/users/logout">Logout</a>
        {{else}}
        <a href="/users/login">Login</a> <!-- Updated link -->
        <a href="/users/signup">Sign Up</a> <!-- Updated link -->
        {{/if}}
    </nav>

    <h1>Welcome to our Blog!</h1>

    <section>
        {{#each posts}}
        <article>
            <h2><a href="/post/{{this.id}}">{{this.title}}</a></h2>
            <p>By: {{this.username}}</p>
            <p>Date: {{this.date_created}}</p>
        </article>
        {{/each}}
    </section>
</body>
</html>
